export function insertProductsToWishlist() {
    // document.addEventListener("DOMContentLoaded", () => {
    //     const wishlistWrap = document.querySelector(".account__product-list--wishlist");
    //     const wishlist = JSON.parse(localStorage.getItem("wishlist")) || [];

    //     wishlist.forEach(id => {
    //         const product = createProductElement(id);
    //         wishlistWrap.append(product);
    //     });

    //     function createProductElement(id) {
    //         const product = document.createElement("div");
    //         product.textContent = `товар ID ${id}`;

    //         return product;
    //     }
    // });
}